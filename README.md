# BruBudget Instructions

1. Clone app to `ixperience` folder.
2. Run `rake db:migrate` and `rake db:seed`.
2. Install Devise to create user accounts.
3. SUGGESTED BONUS: Create appropriate associations between user and expenditure, and only a user to CRUD his or her own expenditures.
4. OPTIONAL BONUS: Install Bootstrap